# Leonardo Settings — The Voynich Manuscript

All 13 images were provided as pre-existing assets, not generated through
this pipeline's Leonardo AI step — the actual generation settings used
(if any) are unknown. `visual_prompts.json`'s `leonardo_settings` per scene
uses placeholder values (`aspect_ratio: "16:9"` matching the project's video
dimensions, `guidance_scale: 7`, `seed`/`style_reference: null`) for schema
completeness, not as a record of real generation parameters.
