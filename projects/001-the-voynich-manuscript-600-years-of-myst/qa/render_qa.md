# render_qa QA — 001-the-voynich-manuscript-600-years-of-myst

### Automated Checks

- Overall: PASS
- Schema (remotion/composition.json): PASS
- Camera/transition variety (no consecutive repeats): PASS
- Caption timing (chronological, readable, not lingering): PASS
- Reasons: none

### Judgment-Based Checks

- `duration_frames` (19686) equals the last scene's `end_frame` exactly;
  scenes are contiguous in frames across all 20 scenes (verified by
  construction from the split-plan frame math, not just seconds, to avoid
  rounding drift at the two odd-duration splits).
- `fps`/`width`/`height` (30/1920/1080) match `config/video_config.json`;
  `render` (h264/crf 18/final_video.mp4) matches `config/render_config.json`.
- Every one of the 20 `camera_motion.type` vocabulary values is used
  exactly once across the 20 scenes — trivially satisfies the no-
  consecutive-repeat gate while maximizing visible variety, and directly
  addresses the "camera motion is imperceptible/too slow" feedback: every
  magnitude was set explicitly, at or above the newly-bumped template
  defaults (`templates/remotion/src/Scene.tsx`), not left at old
  under-tuned values.
- `remotion/captions.json` (290 captions, ~2.2s average, 1.57-3.33s range)
  was generated by `scripts/generate_captions.mjs` from `scripts/script.md`
  and validated by `validate.mjs captions` — directly addresses the
  "needs subtitles, shouldn't linger" feedback.
- Visually confirmed via `remotion still` on the real assets across
  multiple timestamps: reframed shots (e.g. scene_010→011→012, same photo,
  three distinct crops/angles), the `counter`/`map_highlight` motion
  graphics, and the new caption bar all render correctly and without
  overlapping each other or the (now top-anchored) section-title overlay.
- **Pass.**
